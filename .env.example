# Server Configuration
PORT=3001

# Database Configuration (MUST BE THE SAME as login-server)
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=mve_login_db

# JWT Secret (MUST BE THE SAME as login-server)
JWT_SECRET=your_secret_key_here_make_it_long_and_random

# File Server Path (where audio and model files are stored)
FILE_SERVER_PATH=./files
